package br.com.alex.imdbstudycase.home

import org.junit.Test
import org.junit.runner.RunWith
import org.junit.runners.JUnit4

@RunWith(JUnit4::class)
class HomeViewModelTest {

    @Test
    fun `when movies list is received, it should send list to view`() {

    }

    @Test
    fun `when searched movies list is received, it should send list to view`() {

    }

    @Test
    fun `when movies service returns error, it should send error data to view`() {

    }

    @Test
    fun `when search movies service returns error, it should send error data to view`() {

    }
}